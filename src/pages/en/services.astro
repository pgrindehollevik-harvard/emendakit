---
import { getCollection } from "astro:content";
import BaseLayout from "~/layouts/BaseLayout.astro";

const services = await getCollection(
  "services",
  ({ data }) => data.locale === "en",
);
const customService = services.find((service) => service.slug === "custom-kit");

if (!customService) {
  throw new Error("Missing English service entry: custom-kit");
}

const serviceCards = [customService];
---

<BaseLayout
  locale="en"
  altUrl="/tjenester"
  title="Services — custom medical kits from EmendaKit"
  description="EmendaKit co-designs custom emergency medical kits for health trusts, maternity units, vaccination sites and departments with specific needs."
>
  <section class="hero">
    <div class="container hero-grid">
      <div>
        <p class="eyebrow">Services · Emergency medical kits</p>
        <h1>Custom kits for departments with different requirements.</h1>
      </div>
      <p class="lede">
        When the standard kit doesn't fit your needs, we use the EmendaKit
        framework to design one around your procedures, rooms and clinical
        scenarios.
      </p>
    </div>
  </section>

  <section class="service-list" aria-labelledby="service-heading">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">One framework · several clinical contexts</p>
        <h2 id="service-heading">Services</h2>
      </div>

      <div class="cards">
        {
          serviceCards.map((service) => (
            <article class="service-card">
              <div class="card-media" aria-hidden="true">
                <span class="media-label">Design conversation</span>
                <span class="media-line" />
                <span class="media-label">Pilot</span>
                <span class="media-line" />
                <span class="media-label">Delivery</span>
              </div>
              <div class="card-body">
                <p class="card-kicker">{service.data.audience}</p>
                <h3>{service.data.title}</h3>
                <p>{service.data.summary}</p>
                <a href="/en/services/custom-kit" class="card-link">
                  See how we work →
                </a>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="quote-band">
    <div class="container quote-grid">
      <div>
        <p class="eyebrow">A practical starting point</p>
        <h2>Start with a free design conversation.</h2>
      </div>
      <div>
        <p>
          Bring the scenarios, deviations and requirements that make your
          department a little different. We sketch a proposal before you decide
          on a pilot.
        </p>
        <a
          href={`mailto:kjersti@emendakit.com?subject=${encodeURIComponent("Design conversation about a custom kit")}`}
          class="button"
        >
          Email Kjersti →
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .container {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 32px;
  }

  .eyebrow {
    margin: 0 0 14px;
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.12em;
    color: var(--color-navy);
    text-transform: uppercase;
  }

  .hero {
    padding: 88px 0 72px;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(280px, 0.75fr);
    gap: 56px;
    align-items: end;
  }

  .hero h1,
  .section-head h2,
  .quote-band h2 {
    font-family: var(--font-display);
    font-weight: 500;
    letter-spacing: -0.03em;
    color: var(--color-ink);
  }

  .hero h1 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(42px, 6vw, 72px);
    line-height: 0.98;
  }

  .lede {
    margin: 0;
    color: var(--color-ink-2);
    font-size: 19px;
    line-height: 1.55;
  }

  .service-list {
    padding: 76px 0 88px;
    border-top: 1px solid var(--color-rule);
    background: var(--color-cyan-soft);
  }

  .section-head {
    max-width: 720px;
    margin-bottom: 32px;
  }

  .section-head h2 {
    margin: 0;
    font-size: clamp(32px, 4vw, 48px);
    line-height: 1.04;
  }

  .cards {
    display: grid;
    gap: 24px;
  }

  .service-card {
    display: grid;
    grid-template-columns: minmax(260px, 0.85fr) minmax(0, 1.15fr);
    overflow: hidden;
    border: 1px solid var(--color-rule);
    border-radius: var(--radius-xl);
    background: var(--color-surface);
  }

  .card-media {
    display: grid;
    align-content: center;
    gap: 18px;
    min-height: 320px;
    padding: 40px;
    background: linear-gradient(180deg, #17243a 0%, #09111e 100%);
    color: var(--color-bg);
  }

  .media-label {
    width: fit-content;
    border: 1px solid rgba(141, 196, 205, 0.45);
    border-radius: var(--radius-pill);
    padding: 9px 14px;
    font-size: 13px;
    font-weight: 500;
  }

  .media-line {
    width: 1px;
    height: 42px;
    margin-left: 18px;
    background: rgba(141, 196, 205, 0.55);
  }

  .card-body {
    padding: 42px;
  }

  .card-kicker {
    margin: 0 0 12px;
    color: var(--color-ink-3);
    font-size: 13px;
  }

  .card-body h3 {
    margin: 0 0 16px;
    font-family: var(--font-display);
    font-size: clamp(28px, 3.2vw, 40px);
    font-weight: 500;
    letter-spacing: -0.025em;
  }

  .card-body p {
    max-width: 680px;
    margin: 0 0 26px;
    color: var(--color-ink-2);
    font-size: 16px;
    line-height: 1.65;
  }

  .card-link {
    color: var(--color-navy);
    font-weight: 600;
  }

  .quote-band {
    padding: 72px 0;
    border-top: 1px solid var(--color-rule);
  }

  .quote-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(280px, 0.8fr);
    gap: 48px;
    align-items: start;
  }

  .quote-band h2 {
    margin: 0;
    font-size: clamp(32px, 4vw, 48px);
    line-height: 1.04;
  }

  .quote-band p:not(.eyebrow) {
    margin: 0 0 24px;
    color: var(--color-ink-2);
    font-size: 16px;
    line-height: 1.65;
  }

  .button {
    display: inline-flex;
    align-items: center;
    border-radius: var(--radius-pill);
    background: var(--color-navy);
    color: var(--color-bg);
    padding: 14px 22px;
    font-weight: 500;
  }

  @media (max-width: 860px) {
    .hero-grid,
    .service-card,
    .quote-grid {
      grid-template-columns: 1fr;
    }

    .hero {
      padding-top: 64px;
    }

    .card-media {
      min-height: 240px;
    }
  }
</style>
